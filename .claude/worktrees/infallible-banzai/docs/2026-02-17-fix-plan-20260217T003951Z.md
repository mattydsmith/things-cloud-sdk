# Fix Plan (2026-02-17)

## Priority Order

1. Stabilize writes under concurrency
- File: `server/write.go`
- Add a package-level mutex and lock `historyWrite(...)` so `history.Sync()` + `history.Write(...)` is atomic.
- Goal: prevent concurrent `LatestServerIndex` races.

2. Make invalid input fail loudly
- File: `server/write.go`
- Return `400` for invalid `when`, invalid `deadline`, invalid `scheduled` formats instead of silent fallback.
- Update `createTask`, `editTask`, and `createProject` to reject bad date/schedule input.

3. Unify server wire format with CLI
- File: `server/write.go`
- Replace `map[string]any` payloads in `createArea`/`createTag` with typed structs.
- Include `xx` extension consistently and send nullable fields (`sh`) as `null` when unset.

4. Handle sync failures explicitly
- Files: `server/main.go`, `server/mcp.go`, `server/write.go`
- Stop ignoring `syncer.Sync()` errors in read handlers.
- Return an explicit error/warning when refresh fails.
- Log and surface post-write refresh failures.

5. Fix debug nuke flow state corruption
- File: `server/main.go`
- In `/api/debug/nuke-account`, reinitialize global `client`, `history`, and `syncer` after signup/confirm, or remove endpoint in production builds.

6. Add auth guard for MCP (optional)
- File: `server/main.go`
- Add optional bearer-token middleware for `/mcp` (env-driven).

7. Close remaining CLI batch gaps
- File: `cmd/things-cli/main.go`
- Ensure all batch operations validate UUIDs uniformly.
- Verify recent branch updates fully cover complete/trash/purge/move/edit.

## Verification After Each Phase

1. `go test ./...`
2. `./tests/test-smoke.sh`
3. `./tests/test-mcp.sh <cycle>`
4. `./tests/test-mcp-read.sh`
5. `./tests/test-api.sh <base_url> <api_key>`
