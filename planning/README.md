# Planning

Working design docs, implementation plans, and mockups live here.

- `docs/` is for stable reference material such as installation, SDK usage, endpoints, and skills.
- `planning/` is for in-progress design work, technical plans, and visual exploration.

Current initiative folders:

- `persistent-sync-engine/`
- `web-ui/`

This split is intentionally lightweight. If more planning work accumulates, we can keep grouping it by initiative here rather than expanding `docs/` with transient material.
